import { CollectionDefinition } from "postman-collection";

// const coll: CollectionDefinition = require("../../postman-collections/example_postman_collection.json")

export const config = {
  outDir: "./src/examples/out/text-files"
}
